spring.application.name=drillbi backend
spring.datasource.url=jdbc:h2:file:/srv/drillbi/data/quiz_api;AUTO_SERVER=TRUE
spring.datasource.username=sa
spring.datasource.password=
spring.datasource.driver-class-name=org.h2.Driver
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect

spring.jpa.hibernate.ddl-auto=update
spring.sql.init.mode=never

# Google OAuth2 configuration
spring.security.oauth2.client.registration.google.client-id=${GOOGLE_CLIENT_ID}
spring.security.oauth2.client.registration.google.client-secret=${GOOGLE_CLIENT_SECRET}
spring.security.oauth2.client.registration.google.scope=openid,profile,email

# JWT secret loaded from environment variable
jwt.secret=${JWT_SECRET}

# Frontend URL for redirection after successful OAuth2 login
#frontend.url=https://david-andreasson.github.io/quiz_frontend/

# Enable session cookies
server.servlet.session.cookie.same-site=none
server.servlet.session.cookie.secure=true

#Enable dev environment
#spring.profiles.active=dev

openai.api.key=${OPENAI_API_KEY}
anthropic.api.key=${ANTROPIC_API_KEY}

#Enable logging
spring.security.debug=true
logging.level.root=DEBUG
logging.level.org.springframework=DEBUG
logging.level.org.springframework.security=DEBUG
logging.level.org.springframework.security.oauth2=DEBUG
logging.level.org.springframework.security.oauth2.client=DEBUG
logging.level.org.springframework.web.client.RestTemplate=DEBUG
logging.level.com.davanddev.drillbi_backend=DEBUG

frontend.redirect.url=https://drillbi.se
app.jwt.secret=${JWT_SECRET}

# Logga till fil med rotation
logging.file.name=logs/spring.log

logging.file.max-size=10MB
logging.file.max-history=5
logging.pattern.file=%d{yyyy-MM-dd HH:mm:ss} %-5level %logger{36} - %msg%n

# Stripe integration
stripe.secret.key=${STRIPE_SECRET_KEY}
stripe.webhook.secret=${STRIPE_WEBHOOK_SECRET}